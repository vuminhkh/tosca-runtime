package tosca.relationships;

/**
 * This kind of relationship impact scaling.
 * When the source and the target are scaled, the relationship will always be generated between all source and target instances.
 * A relationship non many to many will only have a scope local to the scaling group.
 *
 * @author Minh Khang VU
 */
public interface ManyToMany {
}
