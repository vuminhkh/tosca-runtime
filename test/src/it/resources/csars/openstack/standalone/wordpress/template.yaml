tosca_definitions_version: tosca_simple_yaml_1_0_0_wd03
description: Wordpress partial topology for openstack
template_name: wordpress-template-openstack-sa
template_version: 1.0.0-SNAPSHOT
template_author: MKV

imports:
  - wordpress-template:*
  - toscaruntime-openstack-provider-types:*
  - toscaruntime-script-plugin-types:*

topology_template:

  inputs:
    os_arch:
      type: string
      required: false
    image:
      type: string
    flavor:
      type: string
    plugin_configs:
      type: com.toscaruntime.common.datatypes.PluginConfiguration
    key_pair_name:
      type: string
    security_group_names:
      type: list
      entry_schema:
        type: string
    external_network_name:
      type: string
    cloud_init:
      type: string

  node_templates:

    publicNetwork:
      type: com.toscaruntime.openstack.nodes.ExternalNetwork
      properties:
        network_name: { get_input: external_network_name}

    privateNetwork:
      type: com.toscaruntime.openstack.nodes.Network
      properties:
        network_name: wordpressStandAlone
        cidr: "192.168.1.0/24"

    computeDb:
      type: com.toscaruntime.openstack.nodes.Compute
      properties:
        image: { get_input: image }
        flavor: { get_input: flavor }
        key_pair_name: { get_input: key_pair_name }
        security_group_names: { get_input: security_group_names }
        plugins: { get_input: plugin_configs }
        user_data: { get_input: cloud_init }
      requirements:
        - network: publicNetwork
        - network: privateNetwork

    computeWww:
      type: com.toscaruntime.openstack.nodes.Compute
      properties:
        image: { get_input: image }
        flavor: { get_input: flavor }
        key_pair_name: { get_input: key_pair_name }
        security_group_names: { get_input: security_group_names }
        plugins: { get_input: plugin_configs }
        user_data: { get_input: cloud_init }
      requirements:
        - network: publicNetwork
        - network: privateNetwork