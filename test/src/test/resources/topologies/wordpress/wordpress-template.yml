tosca_definitions_version: tosca_simple_yaml_1_0_0_wd03
description: TOSCA simple profile with a topology template for wordpress
template_name: wordpress-template
template_version: 1.1.0-SNAPSHOT
template_author: FastConnect

topology_template:
  description: Wordpress deployment template
  outputs:
    wordpress_url:
      description: The URL to access the wordpress
      value: { concat: ["http://", get_attribute: [computeWww, ip_address], ":", get_property: [apache, port], get_property: [wordpress, context_path] ] }

  node_templates:
    wordpress:
      type: alien.nodes.Wordpress
      requirements:
        - host: apache
        - database: 
            node: mysql
            capability: alien.capabilities.MysqlDatabase
        - php: 
            node: php
            capability: alien.capabilities.PHPModule
    php:
      type: alien.nodes.PHP
      requirements:
        - host: computeWww
    computeDb:
      type: com.toscaruntime.docker.nodes.Container
      properties:
        image_id: phusion/baseimage
    computeWww:
      type: com.toscaruntime.docker.nodes.Container
      properties:
        image_id: phusion/baseimage
        exposed_ports: 80
        port_mappings: 80 - 51000
    apache:
      type: alien.nodes.Apache
      properties:
        port: 80
        document_root: "/var/www"
      requirements:
        - host: computeWww
    mysql:
      type: alien.nodes.Mysql
      requirements:
        - host: computeDb
