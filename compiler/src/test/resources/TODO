1.Documentation
  - Getting started
  - Openstack provider
  - Docker provider

2.Tosca support:
  - Authorize space in yaml key
  - Authorize null yaml key ?
  - Semantic analyzer completion
    + Input should be compatible with property that perform get_input
    + Function validation for operation
    + Function validation for output
    + Validate all types non abstract before deployment
    + Validate scaling policy
    + Requirement satisfaction validation
    + Node filter
  - Requirement properties
  - Relationship properties - Test
  - Function in node type properties declaration
  - Custom workflow

3.Core:
  - Review class loading model
  - CLI handle correctly errors in rest call
  - Refactoring
  - Ability to cancel running execution

4.Provider:
  - Openstack:
    + Test exit with error
    + Manage public_ip for docker on swarm
    + Windows support
  - Docker:
    + Review status code and outputs detection
    + Test exit with error

5.Runtime:
  - Agent State Persistence
  - Custom command
  - Policy handling (auto-healing, dynamic scaling ... )
  - WinRMI + Batch script
  - Puppet recipe
  - Chef recipe
  - Ansible recipe

6.Testing:
  - Integration end to end test
  - Perform cli test on linux system (Ubuntu)

7.Security:
  - Https bootstrap for proxy and docker
