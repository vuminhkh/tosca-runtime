1.Documentation
  - Getting started
  - Openstack provider
  - Docker provider

2.Tosca support:
  - Authorize space in yaml key
  - Authorize null yaml key ?
  - Implement macro
  - Implement error / warning / info level in semantic analyzer
  - Allow in non strict mode key which is not recognized
  - Verify dsl version to throw error if necessary
  - Semantic analyzer completion
    + Input should be compatible with property that perform get_input
    + Validate scaling policy
    + Requirement satisfaction validation
    + Node filter
    + Component which is not hosted / Relationship whose source and target are not hosted, should not contains artifacts
  - Requirement properties
  - Relationship properties - Test
  - Function in node type properties declaration
  - Custom workflow

3.Core:
  - Review class loading model

4.Provider:
  - Openstack:
    + Test exit with error
    + Manage public_ip for docker on swarm
    + Windows support
  - Docker:
    + Review status code and outputs detection
    + Test exit with error

5.Runtime:
  - Custom command
  - Policy handling (auto-healing, dynamic scaling ... )
  - WinRMI + Batch script
  - Puppet recipe
  - Chef recipe
  - Ansible recipe

7.Security:
  - Https bootstrap for proxy and docker